# Start the Execution Goal

Use this only after `DISCOVERY GATE` and `ARCHITECTURE GATE` records have passed with evidence and required authority, milestones are dependency-ordered, and the current milestone has observable acceptance criteria. Paste the following into Codex; it stays well below the current 4,000-character `/goal` limit.

```text
/goal Use $goal-driven-engineering to execute the approved product goal in PLAN.md to verified completion. Treat PLAN.md as the durable verified-state authority and reconcile it with repository evidence before acting. Confirm Discovery and Architecture gate records, then follow milestone dependency order and the first open criterion. Before each atomic Writer mutation, require clean Allowed Paths, capture Prepared HEAD and the protected-baseline fingerprint, and persist `.goal/execution-state.md` as PREPARED. Persist VERIFYING before safe-to-repeat verification; after success record evidence and the verified-mutation fingerprint as VERIFIED. FINALIZE only when that fingerprint still matches: update and read back durable PLAN evidence, then delete the journal. Journal absence is IDLE; unknown state, scope/HEAD/baseline conflicts, or unknown external non-idempotent results are BLOCKED. Derive only CONTINUE, RETRY_SAFE_UNIT, VERIFY, FINALIZE, or BLOCKED; never rely on prior chat or treat the journal as accepted progress. Independently review significant milestones, repair ordinary failures and valid findings, preserve accepted evidence, and complete system verification and final adversarial review. Hard stop for irreconcilable state, a material human decision, irreversible/destructive action, new authority, unavailable required dependency, or unverifiable acceptance. Do not broaden permissions or silently absorb scope changes.
```
