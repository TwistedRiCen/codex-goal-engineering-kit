# Resume a Goal-Driven Project

```text
Use $goal-driven-engineering to resume this repository without relying on prior chat history. Read in order: applicable AGENTS.md files and repository instructions; PLAN.md; `.goal/execution-state.md` if present; Git branch/HEAD/status/history/current diff; then relevant verification evidence. Treat PLAN as the durable verified-state authority, Git as repository fact, and the journal only as volatile execution intent. Reconcile the approved six states and derive exactly one action from CONTINUE, RETRY_SAFE_UNIT, VERIFY, FINALIZE, or BLOCKED. Journal absence is IDLE. Fail closed for malformed state, Prepared HEAD or protected-baseline drift, out-of-scope mutation, unknown external non-idempotent results, or durable-state conflict. Never promote work without matching verified-mutation evidence. Do not fan out readers by default; use at most one read-only reader only for a named evidence gap. Follow only the derived action, preserve Frozen Decisions and existing changes, do not bypass gates, and update PLAN only with durable verified progress. Pause for BLOCKED state, a material human decision, irreversible/destructive action or new authority, an unavailable required dependency, or unverifiable acceptance; otherwise repair ordinary failures and continue.
```
